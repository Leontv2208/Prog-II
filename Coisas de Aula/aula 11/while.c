#include <stdio.h>

int main(void)
{
    int i = 10;
    printf("No começo i vale %d\n", i);
    while (i >= 0) {
        print("%d\n", i);
        i--1;
    }
    printf("No final i vale %d\n", i);
    return 0;
}